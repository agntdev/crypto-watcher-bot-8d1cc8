[
  {
    "name": "Owner Dashboard shows stats",
    "steps": [
      {
        "send": { "callback": "owner:dashboard" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Owner dashboard\n\nTotal users: 0\n\nTop alerts:\nNo alerts recorded yet.\n\nPrice feed:\n• Base: https://api.coingecko.com/api/v3\n• VS: USD\n• Retries: 3\n\nSystem: scheduler runs alerts, quiet-hour flush, and morning summaries each minute."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "Owner Dashboard counts a user after /start",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [{ "method": "sendMessage" }]
      },
      {
        "send": { "callback": "owner:dashboard" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Owner dashboard\n\nTotal users: 1\n\nTop alerts:\nNo alerts recorded yet.\n\nPrice feed:\n• Base: https://api.coingecko.com/api/v3\n• VS: USD\n• Retries: 3\n\nSystem: scheduler runs alerts, quiet-hour flush, and morning summaries each minute."
            }
          }
        ]
      }
    ]
  }
]
